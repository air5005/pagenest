# PageNest 1.17 阅读进度与索引性能验证

## 修复内容

- 首次全书字数索引不再固定等待 1.5 秒，当前章节可用后立即在后台开始。
- 同一本书已有活动索引时不重复启动；数据库已有总字数时完全跳过全书索引。
- 索引期间拖动阅读进度会保存最后一次目标，不再丢弃操作；索引结束后自动跳转。
- 索引完成后重新读取数据库章节进度，解决当前页面继续使用旧章节列表而无法定位的问题。
- 切换书籍或退出阅读页会取消旧任务并清除待处理进度，避免串书跳转。

## TDD 覆盖

- `PendingReaderProgressTest`：最后一次拖动覆盖、范围限制、切书清理。
- `BookWordIndexPolicyTest`：已有缓存跳过、同书单任务、换书允许新任务。
- `ReaderProgressNavigatorTest`：章节定位和控制器拒绝边界。
- 发布门禁已通过：基础、解析、阅读和应用单元测试，Debug 主 APK、Debug 测试 APK 及 Lint，共 393 个 Gradle 任务成功。
- 本地 APK 元数据：`com.air5005.pagenest`、versionCode 18、versionName `1.17.260827`、minSdk 29、targetSdk 36。
- 本地 APK SHA-256：`b2f69a22760291be8a9e6d562d0d9bc0e058698f984f59842b2508cb9bd7b752`。

## 真机回归

`adb devices -l` 未列出设备，因此本阶段不声明真机通过。安装后用《程序员的思维修炼》验证：首次打开立即显示正文；解析期间连续拖动进度；解析完成自动到最后选择位置；退出重进不再重复解析；连续上下翻页及调整字体不闪退。

## 版本

- versionCode：18
- versionName：`1.17.260827`
- 标签：`pagenest-v1.17.260827`
