# 阅读进度与索引性能实施计划

1. 提取可单测的待处理进度状态机，先写索引中排队、覆盖、完成执行和切书清理测试。
2. 在 `MainReadViewModel` 接入状态机，使索引结束后只应用最后一次拖动目标。
3. 为索引任务增加单任务身份和生命周期控制，移除固定延迟并复用持久化结果。
4. 增加重复加载、取消和偏好变化不重启索引的回归测试。
5. 运行阅读模块及全工程验证，更新真机回归清单。
6. 更新版本，提交并推送 `master`，创建 GitHub Release 并归档 APK。
